# Build context = repo root. Exclude everything pnpm/tsc regenerate inside the
# image, secrets, and local scratch. pnpm-lock.yaml is NOT excluded (needed for
# a frozen install).
node_modules
**/node_modules
**/dist
.git
.env
**/.env
**/.env.local
*.log
**/*.log
packages/api/uploads
packages/api/data
uploads
data
.pnpm-store
.playwright-mcp
*.jpeg
*.png
.pageloop
pageloop.json
.agents
.continue
skills
skills-lock.json
.claude
